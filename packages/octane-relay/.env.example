# Required: Fee payer secret key
# Can be base58 encoded or JSON array format
# Example: FEE_PAYER_SECRET="[1,2,3,...]"
FEE_PAYER_SECRET=

# Optional: Custom RPC endpoint
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Network (devnet or mainnet-beta)
# SOLANA_NETWORK=devnet

# Optional: Rate limit requests per minute per IP
# RATE_LIMIT=60
