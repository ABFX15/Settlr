import { HeroLight } from "@/components/landing/HeroLight";

/**
 * Preview route for the light, Aeropay-style hero — compare against the live
 * dark hero at "/". Not linked anywhere; just for design comparison.
 */
export default function HeroLightPreview() {
  return <HeroLight />;
}
